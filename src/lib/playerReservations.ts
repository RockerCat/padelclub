// Contenido canónico movido a shared/reservations/playerReservations.ts —
// la misma fuente que mobile ahora también usa. Re-export puro para que
// ningún import existente de "@/lib/playerReservations" tenga que
// cambiar.
export * from "../../shared/reservations/playerReservations";
